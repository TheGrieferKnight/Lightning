import { AppSettings } from "../types/settings";

export const mockSettings: AppSettings = {
  autoStart: false,
  overlays: {
    summonerSpells: true,
    liveGame: true,
    matchHistory: false,
  },
  theme: "dark",
  notifications: true,
};
