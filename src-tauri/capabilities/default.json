{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["LightningDashboard", "SumSpellOverlay"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:allow-set-position"
  ]
}
